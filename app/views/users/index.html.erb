<!DOCTYPE html>
<html>
<head>
  <title>All Users</title>
</head>
<body>
  <h1>All Users</h1>
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= link_to "#{user.name} (Number of posts: #{user.post_counter})", user_path(user) %>
      </li>
    <% end %>
  </ul>
</body>
</html>
